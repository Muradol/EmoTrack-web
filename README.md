# Quick Start

## build project
```bash
# install dependency
npm install 

# run
npm run dev
```

## faq

### commit problem
if you meet commit fail, try to add `--no-verify` in commit argues

```bash
git commit -m "commit message" --no-verify
```